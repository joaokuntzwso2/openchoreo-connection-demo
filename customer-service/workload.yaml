apiVersion: openchoreo.dev/v1alpha1

metadata:
  name: customer-service

endpoints:
  - name: api
    visibility:
      - project
    port: 8080
    type: HTTP
    basePath: /customers
    displayName: Customer API